@model IEnumerable<ProjectCoach.Models.Equipo>

@{
    ViewBag.Title = "Mis equipos";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Crear equipo", "CreateOwn", null, new { @class = "btn btn-primary"})    
</p>

@Html.Partial("EquiposPartial", Model)
